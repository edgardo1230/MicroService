#Application Name
spring:
  application:
    name: fp-eureka

#HTTP Server
server: 
  port: 1111

eureka:
  client:  # Not a client, don't register with yourself
    registerWithEureka: false
    fetchRegistry: false
  instance: 
    hostname: localhost 